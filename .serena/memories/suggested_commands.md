Useful Darwin commands: `ls`, `find`, `grep`, `git`, `cd`, `pwd`, `sed`, `awk`, `jq` (if installed), `env`, `printenv`.
Current project inspection: `find . -maxdepth 3 -type f`, `ls -la`, `cat skills-lock.json`.
Likely future automation commands for this repo:
- `npm ci`
- `npm run build`
- `npm test`
- `npm run dry-run`
- `npm run trade:once`
- `npm run report:daily`
- `npm run report:monthly`
- `npx coinone --help` or installed `coinone --help`
No runnable app commands currently exist in the repo.